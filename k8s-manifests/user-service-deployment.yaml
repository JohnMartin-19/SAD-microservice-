# k8s-manifests/user-service-deployment.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: m-farm-user-service
  namespace: default # Ensure this matches your deployment namespace
spec:
  replicas: 2
  selector:
    matchLabels:
      app: m-farm-user-service
  template:
    metadata:
      labels:
        app: m-farm-user-service
    spec:
      # Define the initContainer for database migrations
      initContainers:
        - name: migrate-user-db
          # Use the same application image for running migrations
          image: 740857577909.dkr.ecr.us-east-1.amazonaws.com/m-farm-m-farm-user-service:${{ github.sha }} # Use your ECR image, ideally with github.sha tag
          # The command to run Django migrations
          command: ["python3", "manage.py", "migrate", "--noinput"] # Added --noinput for non-interactive execution
          envFrom: # Load environment variables from ConfigMap and Secret
            - configMapRef:
                name: mfarm-user-config # Reference the ConfigMap
            - secretRef:
                name: mfarm-user-secrets # Reference the Secret
          resources:
            requests:
              cpu: "50m"
              memory: "100Mi"
            limits:
              cpu: "100m"
              memory: "200Mi"

      containers:
      - name: m-farm-user-service
        image: 740857577909.dkr.ecr.us-east-1.amazonaws.com/m-farm-m-farm-user-service:${{ github.sha }} # Use your ECR image, ideally with github.sha tag
        ports:
        - containerPort: 8001 # Based on your previous YAML for user-service. Confirm this is correct.
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        envFrom: # Load environment variables from ConfigMap and Secret
          - configMapRef:
              name: mfarm-user-config # Reference the ConfigMap
          - secretRef:
              name: mfarm-user-secrets # Reference the Secret
        livenessProbe:
          httpGet:
            path: /health 
            port: 8001 
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health 
            port: 8001 
          initialDelaySeconds: 5
          periodSeconds: 5