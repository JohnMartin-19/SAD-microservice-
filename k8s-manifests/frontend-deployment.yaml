apiVersion: apps/v1
kind: Deployment
metadata:
  name: m-farm-frontend
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: m-farm-frontend
  template:
    metadata:
      labels:
        app: m-farm-frontend
    spec:
      containers:
      - name: m-farm-frontend
        image: 740857577909.dkr.ecr.us-east-1.amazonaws.com/m-farm-frontend:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5